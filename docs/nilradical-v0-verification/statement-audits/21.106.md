# Problem 21.106: semantic review and human decision packet

**Agent semantic decision: PASS. Human statement acceptance: PENDING.**

Reviewed 2026-09-16T09:55:59.865023+00:00. This is a new source-to-statement and concrete-model review by the separately tasked statement-audit agent. It does not constitute human approval, a fresh compilation receipt, a novelty decision, or full independent paper-level peer review.

## Source comparison and exact claim

The Notebook asks whether every one-free-variable group formula is concise on residually finite groups: a finite set of satisfying elements should generate a finite subgroup. Conte and Petschick, Conciseness of first-order formulae, Question 1 and its preceding definition explicitly exclude parameters.

An actual parameter-free one-variable formula in the ordinary group language has exactly the two values central(1), central(-1) in the integral Heisenberg group; those values generate an infinite subgroup although the group is residually finite. The formula has prefix exists a,b; forall g,h; exists u,v and an equality-only conjunction.

The [Notebook v46](https://arxiv.org/abs/1401.0300v46), printed page 183, was compared directly with its local PDF text (physical PDF page 183; local extraction (retained privately; [source PDF](https://arxiv.org/pdf/1401.0300v46)), lines 8713–8721). Source SHA-256: `498c2443bcee6ca398430935c87c97b6f99d0b1a6e5cb8f637d188a1827115da`.

The reviewed website summary and scope are preserved in [the mathematical-scope snapshot](sources/reviewed-site-mathematical-scope.json). They match the formal scope:

> A parameter-free first-order formula has exactly two values generating an infinite subgroup in the residually finite integral Heisenberg group.
>
> Disproves universal conciseness for one-variable formulae, using actual first-order syntax and realization. The separate word-conciseness question 21.105 is outside scope.

## Definition, quantifier, and witness checks

1. groupLanguage contains only identity of arity zero, inversion of arity one, and multiplication of arity two, with no relation symbols other than the equality supplied by first-order syntax. The structure interprets these as the given group operations. No Heisenberg element is added as a named parameter.

2. Formula Unit has one free-variable index. The actual bounded formula uses x as the free variable and six bound variables in the intended order. counterexampleFormula is real Mathlib syntax, not a semantic predicate disguised as a formula. realize_counterexampleFormula proves its interpretation for every group.

3. The algebraic FormulaCondition says x=[a,b] and every commutator [g,h] can be represented as [u,b]=[a,v], with [u,a]=[v,b]=1. The realization proof discharges the quantifier-order bridge; moving the x equation outside the universal block is legitimate because groups are inhabited.

4. H is the concrete Heisenberg group of integer triples with multiplication (r,s,t)(r2,s2,t2)=(r+r2,s+s2,t+t2+r*s2), the ordinary integral model. The residual-finiteness instance uses Mathlib Group.ResiduallyFinite and coordinate reduction to finite Heisenberg groups modulo abs(nonzero coordinate)+1.

5. formulaCondition_iff proves the entire value set is exactly the two displayed central elements. central_injective distinguishes them and the integer central embedding gives an injection of Z into their generated subgroup. Finiteness is of the whole formula-value set, and infinitude is of its actual ordinary Subgroup.closure.

6. NotebookStatement adds no nilpotency, finite-generation, torsion, or witness-specific assumptions. It universally quantifies over all residually finite groups and parameter-free formulas in the language. A Type-level concrete counterexample suffices to refute the mathematical universal claim.

7. The final negative theorem is closed; it uses the actual residual-finiteness instance and the syntax-to-semantics equality counterexample_values. It contains no oracle for the formula values or assumed representation of the generated subgroup.

## Public endpoints covered

- `Kourovka.P21_106.counterexample_values`
- `Kourovka.P21_106.counterexample_not_concise`
- `Kourovka.P21_106.not_notebookStatement`
- `Kourovka.P21_106.realize_counterexampleFormula`
- `Kourovka.P21_106.formulaCondition_iff`
- `Kourovka.P21_106.formulaCondition_closure_infinite`
- `Kourovka.P21_106.central_injective`

The [trusted challenge](trusted-challenges/P21_106.lean.txt) independently fixes the reviewed public theorem types; its SHA-256 is `14867f915ac9bae9183204df1e506040ff1f7417c4ea1bf58c6b47203ba82fd5`. Reviewed imported definitions are frozen in the strict-verifier contract. Imported auxiliary endpoints retain the stated source semantics; merely selecting one from an import is not described as an independently rewritten theorem type. The `sorry` bodies belong only to the challenge specification and are never candidate proof evidence.

## Scope limits

This disproves the full formula-conciseness question. It does not resolve the separate word-conciseness question 21.105. No claim is made here about every informal strengthening over all commutative coefficient rings.

The historical record `docs/21.106-verification.md` was read for comparison; its earlier PASS is not the basis of this fresh decision. Strict verification status is recorded separately in the verification receipts.

## Exact frozen statement

[Standalone snapshot](frozen-statements/P21_106.lean.txt).

```lean
import Mathlib.GroupTheory.ResiduallyFinite
import Mathlib.ModelTheory.Semantics

/-!
# Problem 21.106

Conte and Petschick ask whether every parameter-free first-order formula with
one free variable is concise in residually finite groups. Conciseness means
that a finite set of values generates a finite subgroup. The question first
appeared as Question 1 of *Conciseness of first-order formulae* (2025), and was
subsequently included in the Kourovka Notebook as Problem 21.106.

The language below has precisely the usual group operations and no parameters.
Formulas and their interpretations use mathlib's first-order syntax and semantics.
-/

namespace Kourovka.P21_106

/-- The function symbols of the ordinary group language, indexed by arity. -/
inductive GroupFunction : ℕ → Type
  | one : GroupFunction 0
  | inv : GroupFunction 1
  | mul : GroupFunction 2

/-- The ordinary language of groups, with equality and no additional relations. -/
def groupLanguage : FirstOrder.Language :=
  ⟨GroupFunction, fun _ => Empty⟩

/-- Interpret the group language by the given group operations. -/
instance groupLanguageStructure (G : Type*) [Group G] : groupLanguage.Structure G where
  funMap := fun f v => match f with
    | GroupFunction.one => 1
    | GroupFunction.inv => (v 0)⁻¹
    | GroupFunction.mul => v 0 * v 1
  RelMap := fun r => nomatch r

/-- Values of a parameter-free group formula with its sole free variable assigned `x`. -/
def FormulaValues (φ : groupLanguage.Formula Unit) (G : Type*) [Group G] : Set G :=
  {x | φ.Realize (fun _ => x)}

/-- A formula is concise in a group if finite values generate a finite subgroup. -/
def IsConciseIn (φ : groupLanguage.Formula Unit) (G : Type*) [Group G] : Prop :=
  (FormulaValues φ G).Finite → Finite (Subgroup.closure (FormulaValues φ G))

/-- The full question in Kourovka Notebook Problem 21.106. -/
def NotebookStatement : Prop :=
  ∀ (G : Type) [Group G] [Group.ResiduallyFinite G]
    (φ : groupLanguage.Formula Unit), IsConciseIn φ G

end Kourovka.P21_106
```

## Reviewed source hashes

Paths are relative to `kourovka-lean`. Files listed here were inspected for the relevant semantic definitions or endpoint bridges; they are not a claim that every internal proof line has had a new mathematical referee review. The machine-readable packet additionally hashes every Lean source in this problem.

| File | SHA-256 |
| --- | --- |
| `Kourovka/Problems/P21_106/Statement.lean` | `0638a3830d00c8fca4c9f78b7326ce714232c743430535365da4efc6d74d7b8d` |
| `Kourovka/Problems/P21_106/Solution.lean` | `c012b81c5d586631ddc583d4e8ec4549e694bbfe781fc7dc40e570c85f355610` |
| `Kourovka/Problems/P21_106/Proof/Formula.lean` | `cea5bf59d0b98bc594e7b859197731710b5dfad03ad9c3fad2e62afba4b08915` |
| `Kourovka/Problems/P21_106/Proof/Heisenberg.lean` | `57cfe16212a343b8926f699e4ff4e61cc5a5acc5c8bd7cb38ff87f9b1d6691c5` |
| `Kourovka/Problems/P21_106/Proof/Counterexample.lean` | `2e44fa1b381ea69c57d85ace3ee8da524f13477428c5f699d70b00b2528862fd` |
| `.lake/packages/mathlib/Mathlib/GroupTheory/ResiduallyFinite.lean` | `7483fd620fabbe175a1b797e85f32a021a7abfcddb6c5311b13cf52e80674ef2` |

## Human decision record

No human has accepted this snapshot in this run. The human reviewer should record a decision on the precise mathematical statement and scope above, together with the accepted strict-verifier contract hash. A decision on one problem does not approve the others.

- Statement and definition correspondence: **PENDING**.
- Scope and claimed strengthening: **PENDING**.
- Novelty/contribution assessment: **separate review; not decided by this packet**.
- Human reviewer, date, and accepted snapshot/contract hash: **not recorded**.

## Linked technical verification snapshot

This publication adds links to the completed technical evidence; it does not change the semantic review or supply human acceptance.

- [Frozen contract](../p21_106/contract.json), SHA-256 `6fcf1640664a0f919f8a06dd809092168911fca0508b5b595f96c1a991c32347`.
- [Technical receipt](../p21_106/receipt.json), SHA-256 `34dc8394aa553e97ed026dc49d9226b7a38485a637333ee0ecdbe6a9ac434c88`.
- [Explicit checked declarations](../p21_106/explicit-statements.txt).
