# Semantic audit of the final 21.44 formalization

**Verdict: PASS. No material modeling gap found.** This was a read-only audit of the actual final staging definitions and proof instantiations by a separately tasked Codex agent. Integration into the repository was checked to change only module paths and the problem namespace. The [integration manifest](21.44-statement-audit.json) records both source hashes. This is a semantic review of the formalization, not outside human peer review.

## Source question

The literal entry in the [September 2026 Notebook](https://kourovkanotebookorg.wordpress.com/wp-content/uploads/2026/09/21tkt.pdf), Problem 21.44, asks whether the inverse limit of the iterated *natural* permutational wreath products of A5 contains a finitely generated dense subgroup of subexponential growth. The adjacent solved comment belongs to 21.43.

## Exact ambient group and density

- `Wreath.lean` defines the base as `Fin 5 → P`, with `A5 = alternatingGroup (Fin 5)` acting by inverse reindexing. This is the natural degree-five action.
- `InverseLimit.lean` uses `Level 0 = PUnit` and `Level (n+1) = Wreath (Level n)`. `levelProj` deletes the deepest layer recursively and preserves the root action. The harmless level-zero convention gives the required tower starting with A5 at level one.
- `AutTree` is the subgroup of the full dependent product satisfying all adjacent compatibility equations. Every finite level has its discrete topology; `Dense` uses the inherited product/subspace topology. There is no extra restriction to a proper closed subgroup.
- `a` and `b` are fixed compatible sequences. `generatedSubgroup` is their ordinary abstract subgroup closure. `Density.lean` proves full surjectivity on every finite level for this same pair, then topological density by finite-coordinate neighborhoods.

## Ordinary finite generation and growth

- `generatedSubgroup_fg` supplies the actual two-element finite generating set `{a,b}`.
- `GeneratedGeometry.lean` defines the word geometry on that exact subgroup, with finite symmetric generating set `{1,a,a⁻¹,b,b⁻¹}`. Its balls are pointwise powers of this finite set. Including the identity makes radius n mean length at most n, as required for ordinary word balls.
- `HasSubexponentialGrowth` is existential over an honest finite symmetric exhaustive WordGeometry. Its underlying predicate is exactly `Tendsto (fun n => log (volume n) / n) atTop (𝓝 0)`. The value at radius zero does not affect this limit.
- `generatedSection` is the actual section in the same generated group. The proof of section closure uses evaluated finite words, and `root_sections_injective` uses the faithful decomposition of the actual inverse-limit action.

## Complete bridge to the closed endpoint

`Solution.generated_section_shortening` converts the proved section-word witnesses into lengths in the actual word geometry. `Solution.subexponential_growth` instantiates the general counting theorem with those sections, the actual root map, root cardinality 60, and proved alternating representatives of every actual word-ball element.

`Counting.lean` partitions actual word-ball elements into low-variation representatives and a complementary finite set. The low part has an injective word encoding; the high part has a proved section-length budget and uses finite products of actual section balls. `GrowthRate.lean` derives the normalized logarithmic limit from this recurrence and an elementary initial envelope. It does not assume subexponential growth, an unproved growth-rate limit, or a contraction hypothesis.

The final theorem `notebookStatement : NotebookStatement` has no parameters. It explicitly supplies `generatedSubgroup`, finite generation, `dense_generated_pair`, and the proved `wordGeometry` growth result. Thus every mathematical premise of the general criteria is discharged for the concrete witness.

## Scope

The formalized result answers the Notebook existence question affirmatively. It proves qualitative subexponential growth; it does not claim the separate explicit constant-200 upper bound or novelty/priority. This is the intended scope and is sufficient for the literal question.
