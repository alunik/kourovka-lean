# Independent final semantic audit: Kourovka 21.40

Result: PASS; no mathematical-scope or public-statement defect found in the source snapshot below. This was a read-only source and dependency-definition audit. No proof modules were rebuilt, and this report does not replace the root agent's kernel build/axiom audit.

## Checks

- `HasFiniteAutomorphismOrbits G` uses the quotient for the full `MulAut G` action on `G`. Mathlib's imported action is evaluation by abstract group automorphisms. The hypothesis does not restrict to ambient matrix conjugation, rational algebraic automorphisms, or finitely generated groups.
- `NotebookStatement` quantifies over every natural dimension and arbitrary subgroups of `GL_n(Q)`; `notebookStatement` is an unconditional proof of that exact proposition.
- `StructuralConclusion` asserts `K.Normal` and `K.FiniteIndex` independently of the numerical index bound. Mathlib defines `FiniteIndex` by nonzero index and proves equivalence with a finite quotient, so the infinite-index default value zero cannot satisfy the statement accidentally.
- The change-of-basis quantifiers are `exists P, forall g in K`. The construction applies `exists_conjugating_strict_upper` once to the full trace ideal. It therefore supplies one simultaneous rational invertible matrix, and conjugating `g-1` gives diagonal entries one and all entries below the diagonal zero for every element of the same subgroup K.
- Nilpotency is explicitly asserted in addition to `nilpotencyClass K <= n-1`; the class bound does not use the default value for a non-nilpotent group. The condition `forall g:K, IsOfFinOrder g -> g=1` is full torsion-freeness: mathlib's finite-order predicate is equivalent to a positive power equalling one.
- Dimension zero is included without positivity assumptions. `Fin 0` is empty, its matrix group and all subgroups are trivial, the index bound is 1, and natural subtraction makes the class bound zero. The flag construction includes its subsingleton base case.
- `RationalFlag` correctly uses a nil left ideal. For a nonzero action on a simple module, simplicity gives an element a with ar fixing a nonzero vector; left stability makes ar nilpotent, a contradiction. Finite-length induction combines the previously constructed basis with a rational complement. The two-sided trace ideal satisfies the weaker left-stability assumptions. No algebraic closure or per-element choice of basis occurs.
- `NumberFields` uses faithful restriction of scalars and an isomorphism onto the rational matrix image. It transports the full abstract automorphism-orbit condition through that isomorphism and transports actual finite index, nilpotency and torsion-freeness back to G.
- The `_of_rational_structure` lemmas are explicitly conditional internal bridge lemmas. The public `numberField_structure` and `numberField_virtually_nilpotent` in `Solution.lean` (namespace `Kourovka.P21_40`) discharge that hypothesis using `structural_theorem`. Their statements are unconditional given the intended finite-orbit assumption. They assert a finite-index torsion-free nilpotent subgroup, without incorrectly promising the rational dimension bound over the original number-field dimension.
- All four files were read completely. No `sorry`, new axiom, `native_decide`, or hidden section-variable assumptions occur in them. Compilation was left to the parent as requested.

## Source pin

Timestamp (UTC): 2026-09-15T12:19:31.940691+00:00

- `Kourovka/Problems/P21_40/Statement.lean`: `5eff5e3c95972b9228bc6ce47aff248af6b03dbe76dbbfd5fd6c3f35af4c43a8`
- `Kourovka/Problems/P21_40/Solution.lean`: `713dc8e87e073b12dca18db9efb552101c22d17f5806ebe442c34f40887e5f4d`
- `Kourovka/Problems/P21_40/Proof/RationalFlag.lean`: `c076c4bca90ce98a072377bc05fbc4264e571e0e2a8b02df5ef5ba124cd2f01b`
- `Kourovka/Problems/P21_40/Proof/NumberFields.lean`: `4d56b9a0cf5363ce7b0e56782a5268618914a98118af773d39f07183fbeb9365`

Lean: `v4.34.0-rc2`. Mathlib: `87f6d5ec4c780581c9a78b06a9c5f1cf86dc5a70`.
