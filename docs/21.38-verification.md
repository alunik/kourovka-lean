# Verification of Problem 21.38

[Problem](../Kourovka/Problems/P21_38/README.md) · [Documentation](README.md)

The concrete infinite group of ordinary spread exactly one is formalized in
[Solution.lean](../Kourovka/Problems/P21_38/Solution.lean). Both the original
Donoven–Harper existence question and Notebook 21.38 are closed theorems.
The [machine-readable receipt](21.38-verification.json) records the checked
source snapshot, commands, dependency pins, and log hashes.

## Environment

- Date: 15 September 2026; local macOS execution.
- Lean: `leanprover/lean4:v4.34.0-rc2`.
- mathlib: `87f6d5ec4c780581c9a78b06a9c5f1cf86dc5a70`.
- Base repository commit: `b7ca6c7`.
- Changes remain local and uncommitted; no push was performed.

## Proof and trust checks

The [targeted repository build](21.38-build.txt) succeeded with 1475 Lake
jobs and no warnings. The [full repository build](21.38-full-build.txt)
also passed, with 9305 Lake jobs. The [root guarded axiom
audit](21.38-axiom-audit.txt) exited successfully.
The closed guards for `Kourovka.P21_38.originalQuestion` and
`Kourovka.P21_38.notebookStatement` report exactly:

```text
[propext, Classical.choice, Quot.sound]
```

The generation argument is proved through actual binary branches, interval
motion, finite local interpolation, core commutators, and the endpoint
quotient. No published generation theorem is assumed. In particular,
nonidentity elements with trivial endpoint character are included.
The [independent statement audit](21.38-statement-audit.md) also checked an
expanded existence formulation
using ordinary subgroup closure, infinitude, arbitrary nonidentity inputs,
and a fixed pair with no common companion. A comment- and string-stripped
scan of all 56 Lean sources found no admissions, added axioms, unsafe
declarations, `native_decide`, or `bv_decide`.

## Reproduction

```sh
python3 scripts/check_repository.py
lake exe cache get
LEAN_NUM_THREADS=1 lake build
lake env lean Audit.lean
git diff --check
```

The [source manifest](21.38-source.sha256) covers 61 files: 40 problem Lean
modules, 16 reused foundation modules, the three pinned configuration files,
and the root import and audit files. The proof modules comprise 5,331 new
lines and 5,124 adapted foundation lines. Markdown documentation is outside
this manifest, so wording-only changes do not invalidate the proof snapshot.

The target solution builds without warnings. The full repository run also
checks the existing problems, which retain their pre-existing style and
deprecation warnings (192 replayed warnings); no unrelated proof sources
were changed. All nine dependency checkouts are clean at their manifest
revisions. The repository check passed for seven problems and 38 Markdown
files; the source manifest, whitespace checks, and `git diff --check` passed.

## Imported sources

The 16 reused foundation modules have an exact
[provenance manifest](../Kourovka/External/GroupApproximation/provenance.json),
[license](../Kourovka/External/GroupApproximation/LICENSE), and
[adaptation patch](../Kourovka/External/GroupApproximation/adaptations.patch).
The source pin is `a39c9b72861bd04c71fc8e18876d13307851d777`.
