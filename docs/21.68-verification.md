# Verification of problem 21.68

[Repository](../README.md) · [Problem](../Kourovka/Problems/P21_68/README.md)

## Recorded run: 11 September 2026

The new solution and its independent endpoint audits passed on the local
macOS host. Verification covers problem 21.68 and its supporting modules. The
[machine-readable receipt](21.68-verification.json) records the final status,
commands and source digest. This record supplements the historical
[9 September verification](verification.md); that receipt is unchanged.

The new public endpoint is
`Kourovka.P21_68.not_notebookStatement : ¬ Kourovka.P21_68.NotebookStatement`.
It has no residual hypotheses. Independent checks also confirmed
`card_G : Nat.card G = 2592`, simplicity of `witnessRepresentation`, its degree
eight, and `witness_not_monomial`.

## Environment and source

Lean is `leanprover/lean4:v4.34.0-rc2`, with mathlib
`87f6d5ec4c780581c9a78b06a9c5f1cf86dc5a70`. Every dependency checkout was
verified clean at its manifest revision. The existing exact-pin mathlib cache
was reused; `lake exe cache get` reported no files to download and 8765 already
decompressed files.

The [source manifest](21.68-source.sha256) covers all Lean sources, pinned
configuration, the existing certificate generators and inputs, and the
vendored TauCeti license and provenance inventory. The manifest was recorded
at completion; the solution's proof sources were unchanged after its
successful build. To check the recorded snapshot:

```sh
LC_ALL=C LANG=C shasum -a 256 -c docs/21.68-source.sha256
```

## Mathematical and trust checks

The public semiabelian definition follows the literal notebook wording: an
actual subgroup chain and an abstract quotient of an abelian semidirect
product at each step. The auxiliary abelian groups in the definition are not
restricted to be finite. The constructed example uses finite factors.
Monomiality uses actual induction of linear complex characters, and `Simple`
means ordinary nonzero irreducibility.

The explicit representation uses 64 kernel-checked Gaussian-integer
quaternion products, eight integral intertwining identities, and a 24-term
integer character norm. Finite group identities also use kernel reduction.
There is no new external certificate generator or character-table oracle.
The full original conjecture, rather than a bounded variant, is negated.

The new endpoint, order and degree have each been independently audited with
exactly `propext`, `Classical.choice`, and `Quot.sound`. The
[targeted guarded audit](../Kourovka/Problems/P21_68/Audit.lean) checks these
three theorems without importing the other solutions. The repository's
[full audit](../Audit.lean) also registers the new public endpoint.

All 31 vendored source digests were independently compared with their exact
upstream git objects; author headers, Apache license and local modification
notices were checked. See [TauCeti provenance](../Kourovka/External/TauCeti/README.md).
The new solution and supporting modules compile without warnings.

## Commands

```sh
python3 scripts/check_repository.py
lake exe cache get
LEAN_NUM_THREADS=1 lake build Kourovka.Problems.P21_68.Solution
lake env lean Kourovka/Problems/P21_68/Audit.lean
git diff --check
```

The receipt records a local run, not a remote CI result. The older certificate
generators and inputs were unchanged; their generation was not repeated.
